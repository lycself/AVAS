# Runtime dependencies (see pyproject.toml for the authoritative list).
# Install everything with:  pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
numba>=0.57
pywebview>=5.3
Send2Trash>=1.8
openpyxl>=3.0

# optional
# mpi4py>=3.1        # HPC job submission
# pyinstaller>=6.0   # building the stand-alone executable (packaging/avas.spec)
# pytest>=7          # running tests/
