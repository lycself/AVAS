修复启动后开启或切换代理仍需重启才能检查更新的问题。独立更新窗口补齐深浅主题和动效交接，改善高 DPI 中文与图形清晰度；等待条与主程序一致从左向右循环，不再停住或往返运动。源码及打包更新器统一使用 AVAS 图标。

Update requests refresh proxy settings on every attempt. The independent updater inherits theme and motion, improves high-DPI text and geometry, matches the main window’s left-to-right waiting animation, and uses the AVAS icon for source and packaged installations.
