更新下载遇到暂时网络故障或不完整响应时自动重试，支持同一次更新准备中的安全续传；服务器不支持时重新下载，安装前仍校验完整更新包。下载、解压和备份替换前增加磁盘空间检查，尽量在退出程序前发现空间不足，减少大包更新失败。持续断网、磁盘故障和源码依赖安装失败仍需处理，不承诺所有历史客户端都能自动升级。

下载与准备阶段可取消更新，安全停止后继续使用原版本。目标包下载缓存跨取消和重启保留，再次更新同一附件可续传，校验失败自动重新下载；不同版本不会混用缓存。安装已开始后不能取消，避免留下混合版本。
